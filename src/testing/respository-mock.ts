// essa classe serve para ser injetada e para testar os métodos do service
export const RespositoryMock = {
    /*
    provide: getRepositoryToken(UserEntity),
    useValue: {
        exists: jest.fn(),
        create: jest.fn(),
        save:   jest.fn(),
        find:   jest.fn(),
        findOneBy: jest.fn(),
        update:  jest.fn(),
        delete:  jest.fn()

    }
    */
}